version: '3.8'
services:
  backend:
    build: ./backend/docker
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
  frontend:
    build: ./frontend/docker
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true

